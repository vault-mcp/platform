{
  "name": "@vault-mcp/obsidian-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --minify --platform=node --format=cjs --external:obsidian --outfile=main.js",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "@vault-mcp/core": "0.2.0",
    "@vault-mcp/server": "0.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "obsidian": "^1.8.7"
  }
}
